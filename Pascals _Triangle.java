import java.util.*;
class Pascals_Triangle{
    public class void main(Sting[] args){
       Scanner s=new Scanner (System.in);
       int a=s.nextInt();
       int[] b=new int[a];
       for(int i=0;i<a;i++)
       {
        int[] c=new int[i+2];
        int c[0]=0;
        int c[c.length()]=0;
        for(int j=i;j<c.length()-2;j++)
        {
          c[j]=c[j]+c[j+1];
        }
        c =new int[i]
        b[i]=
       }
    }
}